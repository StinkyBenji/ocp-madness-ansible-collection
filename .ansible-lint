---
profile: basic

exclude_paths:
  - .cache/
  - .github/
  - .tekton/
  - .vscode/
  - changelogs/
parseable: true

var_naming_pattern: "^[a-z_][a-z0-9_]*$"

use_default_rules: true

offline: true
